#include <iostream>
#include <math.h>
using namespace std;

double function(double x){
  return 3 + 200*x - 30*pow(x, 2.) + 4*pow(x, 3.) - pow(x, 4);
}

main(){
  const int N = 100;
  vector<double> xi(N);
  double upper = -10;
  double lower =  10;


  return 0;
}
